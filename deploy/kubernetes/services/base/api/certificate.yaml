apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-cert
spec:
  commonName: api.imagehiding.com
  secretName: api-cert
  dnsNames:
    - api.imagehiding.com
  issuerRef:
    kind: _
    name: _
