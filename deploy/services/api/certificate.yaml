apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-cert
  namespace: services
spec:
  secretName: api-cert
  dnsNames:
    - api.doellmann.xyz
  privateKey:
    algorithm: ECDSA
  issuerRef:
    kind: Issuer
    name: services-issuer
